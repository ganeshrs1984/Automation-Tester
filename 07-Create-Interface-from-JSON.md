
# Lesson 7: Create an Interface from JSON

Given this JSON:

```json
{
    "id": 1,
    "title": "Angular",
    "completed": true
}
```

Create the interface.

```typescript
interface Todo {
    id: number;
    title: string;
    completed: boolean;
}
```

Practice this with many JSON examples until it becomes natural.

---
