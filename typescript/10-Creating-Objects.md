
# Lesson 10: Creating Objects

## Goal

Create instances of a class.

```typescript
const person1 = new Person("Ganesh", 43);

const person2 = new Person("John", 30);
```

Explain:

- `class` is the blueprint.
- `new` creates an object.
- `constructor` initializes the object.
